# Container Audit — acme-api

**Audited:** 21/04/2026
**Mode:** live
**Dockerfiles audited:** 2
**Compose files audited:** 1
**Verdict:** CONDITIONAL — score 58/100

---

## Executive Summary

Two Dockerfiles (`Dockerfile` and `Dockerfile.worker`) and one `docker-compose.yml` were audited. The API image runs as root, ships a 1.4GB image because of a single-stage build that includes the full Node toolchain, and has no HEALTHCHECK. The worker image is better (non-root, multi-stage) but pins its base by tag not digest. Compose uses `privileged: true` on a debug sidecar container — flagged CRITICAL.

**Top three risks:**
1. `Dockerfile:14` — container runs as root *(CT-001)*
2. `docker-compose.yml:33` — `privileged: true` on debug service *(CT-002)*
3. `Dockerfile:7` — `ARG NPM_TOKEN` bakes token into image history *(CT-003)*

---

## Category Scores

| Category | Weight | Score |
|---|---|---|
| A. Base image | 15 | 60 |
| B. User & privileges | 20 | 40 |
| C. Secrets & leaks | 20 | 50 |
| D. Layer efficiency | 10 | 40 |
| E. Signals & shutdown | 5 | 70 |
| F. Healthcheck | 10 | 50 |
| G. `.dockerignore` | 10 | 80 |
| H. docker-compose | 10 | 50 |

---

## Per-Dockerfile Findings

### `Dockerfile` — HIGH RISK

| ID | Sev | Cat | Line | Issue |
|---|---|---|---|---|
| CT-001 | CRITICAL | B.1 | 14 | No `USER` directive |
| CT-003 | HIGH | C.1 | 7 | `ARG NPM_TOKEN` |
| CT-005 | HIGH | D.1 | 1 | Single-stage build ships 1.4GB |
| CT-008 | MEDIUM | A.2 | 1 | `FROM node:20-alpine` (tag pin) |
| CT-011 | MEDIUM | F.1 | — | No HEALTHCHECK |

### `Dockerfile.worker` — LOW RISK

| ID | Sev | Cat | Line | Issue |
|---|---|---|---|---|
| CT-009 | MEDIUM | A.2 | 1 | Tag pin |
| CT-012 | INFO | E.3 | 29 | CMD in shell form |

---

## Compose Findings

| ID | Sev | Line | Issue |
|---|---|---|---|
| CT-002 | CRITICAL | docker-compose.yml:33 | `privileged: true` on `debug` service |
| CT-006 | HIGH | docker-compose.yml:18 | `network_mode: host` on `api` service |
| CT-010 | MEDIUM | docker-compose.yml:42 | No resource limits |

---

## Image Scan *(Trivy)*

| Image | Critical | High | Medium | Low |
|---|---|---|---|---|
| acme-api:local | 2 | 14 | 31 | 87 |
| acme-worker:local | 0 | 3 | 8 | 24 |

Two critical CVEs in `acme-api:local` (both in `libxml2` transitive). Upgrade base image and rebuild.

---

## Risk Register

| ID | Severity | Category | Target | Evidence | Remediation |
|---|---|---|---|---|---|
| CT-001 | CRITICAL | B.1 | Dockerfile:14 | No USER set | `USER 10001:10001` |
| CT-002 | CRITICAL | H.1 | docker-compose.yml:33 | `privileged: true` | Remove; scope caps explicitly |
| CT-003 | HIGH | C.1 | Dockerfile:7 | `ARG NPM_TOKEN` | BuildKit `--mount=type=secret` |
| CT-005 | HIGH | D.1 | Dockerfile:1 | Single-stage 1.4GB | Multi-stage build |
| CT-006 | HIGH | H.2 | docker-compose.yml:18 | `network_mode: host` | Remove; map ports explicitly |

---

## Action Batches

### Critical
- CT-001: add USER directive
- CT-002: remove privileged mode

### High
- CT-003: move to BuildKit secrets
- CT-005: split into builder + runtime stages
- CT-006: remove host networking

### Medium
- CT-008, CT-009: pin base images by digest
- CT-010, CT-011: add resource limits and HEALTHCHECK

---

## Suppressed

None.

---

*Generated by `devops:container-audit` — Anthril DevOps plugin*
