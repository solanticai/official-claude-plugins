# Application Audit — Project Profile

> This file is generated by the `application-audit` skill on first run and
> refreshed when the detected stack drifts from what is recorded here.
> Auditors read this profile **before** investigating, so it is the canonical
> shared context for every agent.
>
> You can hand-edit this file. Wrap any human-authored content between
> `<!-- HUMAN-EDIT-START -->` and `<!-- HUMAN-EDIT-END -->` markers and the
> profile-builder will preserve those blocks on `--update`.

| Field | Value |
|---|---|
| **Profile generated** | {{generated_at}} |
| **Profile updated** | {{updated_at}} |
| **Generator** | `application-audit/profile-builder` |
| **Permissive mode** | {{permissive_mode}} |
| **Stack drift from canonical** | {{drift_summary}} |

---

## 1. Stack

### Runtime & framework

- **Next.js:** {{next_version}} (canonical: 15.x)
- **React:** {{react_version}} (canonical: 19.x)
- **TypeScript:** {{ts_version}} — strict mode: {{ts_strict}}
- **Package manager:** {{package_manager}} ({{lockfile}})
- **Node:** {{node_version}}
- **Monorepo:** {{monorepo_tool}} ({{workspaces_count}} workspaces) or "no"

### Database & data layer

- **Supabase:** {{supabase_detected}} (`@supabase/supabase-js`: {{supabase_js_version}}, `@supabase/ssr`: {{supabase_ssr_version}})
- **Direct Postgres / ORM:** {{orm_summary}} (Prisma / Drizzle / postgres.js / none)
- **Connection mode (declared):** {{connection_mode}} (direct / Supavisor session / Supavisor transaction / mixed / unknown)

### Styling & UI

- **Tailwind:** {{tailwind_version}} — content scan paths: {{tailwind_content}}
- **Component library:** {{component_lib}} (shadcn/ui / Radix / MUI / custom / none)

### Hosting & infra

- **Hosting:** {{hosting_target}} (Vercel / Cloudflare / Fly / self-hosted / unknown)
- **CI:** {{ci_system}}
- **Realtime:** {{realtime_in_use}} (in use / not used / unknown)

---

## 2. Repo Layout

```
{{repo_tree_summary}}
```

Notable directories:

{{notable_dirs}}

---

## 3. Supabase Setup (if detected)

- **Migrations directory:** {{migrations_dir}}
- **Edge Functions directory:** {{functions_dir}}
- **Generated types path:** {{types_path}}
- **Browser client utility:** {{browser_client_path}}
- **Server client utility:** {{server_client_path}}
- **Middleware Proxy:** {{middleware_path}}
- **RLS policies declared:** {{rls_policies_count}} (in migrations) — auditors will verify per-op coverage

---

## 4. Connected MCPs

{{connected_mcps_table}}

> Auditors will use these MCPs in their domain when relevant. Anything missing
> here means the matching auditor degrades to filesystem-only investigation
> and flags the gap.

---

## 5. Memex Integration

- **Mode:** {{memex_mode}} (plugin / wiki / none)
- **Wiki root:** {{memex_wiki_root}}
- **Index page:** {{memex_index_path}}

> When `mode = plugin` or `mode = wiki`, every auditor consults memex before
> filing an open question.

---

## 6. Auditor Calibration Notes

The profile-builder records here any heuristic choices that affect how
individual auditors should interpret evidence. Auditors read this section to
adjust their confidence levels.

{{calibration_notes}}

---

<!-- HUMAN-EDIT-START -->

## 7. Human Notes

> Free-form notes from the project owner. The profile-builder preserves this
> block on every refresh. Anything outside the markers will be regenerated.

{{human_notes_placeholder}}

<!-- HUMAN-EDIT-END -->
