# Auto-generated by skill-eval-bootstrap on 2026-05-20.
# Review and tune judge_criteria + edge expectations before relying on this suite.

skill: plan-orchestrator
description: "Baseline eval suite generated for plan-orchestrator"

test_cases:
  - id: activate-positive-canonical
    kind: activation
    description: "should fire on the canonical use case"
    user_input: "Turns a bullet list of tasks/issues/bugs into one ordered plan with full coverage verification"
    expected_activation: true
  - id: activate-positive-paraphrase
    kind: activation
    description: "should fire when leading verb is swapped (Turns → Turns)"
    user_input: "Turns a bullet list of tasks/issues/bugs into one ordered plan with full coverage verification"
    expected_activation: true
  - id: activate-positive-name-mention
    kind: activation
    description: "should fire when the skill is mentioned by name"
    user_input: "run plan-orchestrator"
    expected_activation: true
  - id: activate-negative-unrelated
    kind: activation
    description: "should NOT fire on unrelated coding question"
    user_input: "how do I configure SSH key forwarding on Windows?"
    expected_activation: false
  - id: activate-negative-near-miss
    kind: activation
    description: "should NOT fire on a query that names a related concept but is out of scope"
    user_input: "remind me what skills are"
    expected_activation: false
  - id: functional-golden
    kind: functional
    description: "produces the artefact described in examples/"
    user_input: "<replace-with-real-bullet list of tasks, issues, notes, or bug reports — one per line, prefixed with * or - or numbered>"
    expected_outputs:
      - kind: file_created
        path_glob: "*.md"
    judge_criteria:
      - "Australian English used throughout the narrative (colour, optimise, behaviour, organise)"
    timeout_seconds: 240
  - id: edge-empty-input
    kind: edge-case
    description: "handles empty input gracefully"
    user_input: ""
    expected_error: "empty-argument"
  - id: edge-nonexistent-target
    kind: edge-case
    description: "handles a target that does not exist"
    user_input: "this-target-definitely-does-not-exist-xyz"
    expected_error: "target-not-found"
