# Plan Orchestrator — Example Output

This file shows the end-to-end output of the skill given a small bullet-list input. Generated by running the actual scripts against synthetic agent reports.

## Input ($ARGUMENTS as typed in Plan Mode)

```
target: ./apps/web

* Add a sign-out button to the user menu
- Fix the 500 on /api/orders when cart is empty
1. Migrate the orders table to add a `currency` column
* Bug: stripe webhook doesn't verify signatures
* Add Playwright e2e tests for the checkout flow
- Update the README with the new env vars
```

## Routing decision (Phase 2)

| Domain | Tasks |
|---|---|
| frontend | T1 |
| backend | T2, T4 |
| database | T3 |
| infrastructure | T6 |
| testing | T5 |
| documentation | T6 |

6 parallel sub-agents dispatched (T6 routed to two agents because it has both `infrastructure` and `documentation` tags — the orchestrator's compiler dedupes the final plan by `(task_id, file)`).

## Coverage (Phase 4)

```json
{
  "covered":     ["T1", "T2", "T3", "T4", "T5", "T6"],
  "missing":     [],
  "duplicates":  [],
  "spurious":    [],
  "coverage_pct": 100.0
}
```

## Final compiled plan (Phase 5)

The plan below is what `ExitPlanMode` receives in Plan Mode.

---

# Plan Orchestrator — Consolidated Plan

| Field | Value |
|---|---|
| **Generated** | 2026-04-25 02:21 |
| **Target** | `./apps/web` |
| **Tasks** | 6 |
| **Domains routed** | 6 |
| **Coverage** | 100.0% (missing: 0, spurious: 0) |

## 1. Task Coverage

| ID | Status | Domains | Original task |
|---|---|---|---|
| T1 | 🟢 covered | frontend | Add a sign-out button to the user menu |
| T2 | 🟢 covered | backend | Fix the 500 on /api/orders when cart is empty |
| T3 | 🟢 covered | database | Migrate the orders table to add a `currency` column |
| T4 | 🟢 covered | backend | Bug: stripe webhook doesn't verify signatures |
| T5 | 🟢 covered | testing | Add Playwright e2e tests for the checkout flow |
| T6 | 🟢 covered | infrastructure, documentation | Update the README with the new env vars |

## 2. Per-Task Plan

### T1 — Add a sign-out button to the user menu

**Domains:** frontend

**Original:** Add a sign-out button to the user menu
**Domain:** frontend
**Confidence:** high

**Investigation summary:**
The user menu lives at `apps/web/src/components/UserMenu.tsx`. It currently exposes profile and settings but no sign-out action. Auth helper is already imported.

**Evidence:**
- `apps/web/src/components/UserMenu.tsx:14` — menu items array missing the sign-out entry

**Proposed steps:**
1. Add a `signOut` MenuItem at `apps/web/src/components/UserMenu.tsx:42` calling the existing `supabase.auth.signOut()` helper.

**Risks:**
- Need to clear local state on sign-out to avoid stale RSC cache.

**Verification:**
- Manual: click sign-out, confirm redirect to `/login`.

### T2 — Fix the 500 on /api/orders when cart is empty

**Domains:** backend

**Original:** Fix the 500 on /api/orders when cart is empty
**Domain:** backend
**Confidence:** high

**Investigation summary:**
The handler at `apps/web/src/app/api/orders/route.ts:23` indexes `cart.items[0]` without a length check.

**Evidence:**
- `apps/web/src/app/api/orders/route.ts:23` — unguarded array access

**Proposed steps:**
1. Add an early `if (!cart.items.length) return new Response(...)` guard at line 22.

**Risks:** None significant.
**Verification:** `curl -X POST .../api/orders -d '{"items":[]}'` should return 400.

### T3 — Migrate the orders table to add a `currency` column

**Domains:** database

**Original:** Migrate the orders table to add a \`currency\` column
**Domain:** database
**Confidence:** high

**Investigation summary:** No `currency` column on `public.orders`.

**Evidence:**
- `supabase/migrations/20260101_create_orders.sql:5`

**Proposed steps:**
1. Add migration `supabase/migrations/20260425_orders_currency.sql` with `ALTER TABLE public.orders ADD COLUMN currency text NOT NULL DEFAULT 'USD'`.

**Risks:** Existing rows backfilled with USD default — confirm with the team.
**Verification:** `supabase db push` and `\d public.orders`.

### T4 — Bug: stripe webhook doesn't verify signatures

**Domains:** backend

**Original:** Bug: stripe webhook doesn't verify signatures
**Domain:** backend
**Confidence:** high

**Investigation summary:**
Webhook at `apps/web/src/app/api/stripe/webhook/route.ts:8` parses body without calling `stripe.webhooks.constructEvent`.

**Evidence:**
- `apps/web/src/app/api/stripe/webhook/route.ts:8`

**Proposed steps:**
1. Use raw body and signature header to verify per Stripe docs.

**Risks:** Replay protection still requires idempotency at app layer.
**Verification:** Stripe CLI `stripe trigger payment_intent.succeeded`.

### T5 — Add Playwright e2e tests for the checkout flow

**Domains:** testing

**Original:** Add Playwright e2e tests for the checkout flow
**Domain:** testing
**Confidence:** medium

**Investigation summary:** No Playwright config in repo.

**Evidence:**
- `package.json:1` — no playwright dep

**Proposed steps:**
1. Add `@playwright/test`, scaffold `e2e/checkout.spec.ts`.

**Risks:** Flake on async cart updates.
**Verification:** `npx playwright test`.

### T6 — Update the README with the new env vars

**Domains:** infrastructure, documentation

**Original:** Update the README with the new env vars
**Domain:** documentation
**Confidence:** medium

**Investigation summary:** README env section last updated 6 months ago.

**Evidence:**
- `README.md:120` — env table missing STRIPE_WEBHOOK_SECRET, SUPABASE_SERVICE_ROLE

**Proposed steps:**
1. Append the missing env vars to the table at `README.md:120`.

**Risks:** None.
**Verification:** Visual diff.

## 3. Aggregated Change Set by File

Files referenced across the per-task plans, sorted alphabetically. If multiple tasks touch the same file, batch the changes.

| File | Tasks |
|---|---|
| `README.md` | T6 |
| `apps/web/src/app/api/orders/route.ts` | T2 |
| `apps/web/src/app/api/stripe/webhook/route.ts` | T4 |
| `apps/web/src/components/UserMenu.tsx` | T1 |
| `e2e/checkout.spec.ts` | T5 |
| `package.json` | T5 |
| `supabase/migrations/20260101_create_orders.sql` | T3 |
| `supabase/migrations/20260425_orders_currency.sql` | T3 |

## 4. Cross-Cutting Concerns

Tasks that span multiple domains. Coordinate the relevant agents' recommendations in your execution.

- **T6** (infrastructure, documentation) — Update the README with the new env vars

## 5. Suggested Execution Order

Apply changes in this order to minimise rework: schema → infra → security → backend → frontend → tests → docs.

1. **Database** — T3
2. **Infrastructure** — T6
3. **Backend** — T2, T4
4. **Frontend** — T1
5. **Testing** — T5
